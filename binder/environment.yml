channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - xarray
  - fsspec
  - zarr
  - s3fs
  - dask
  - ipython
  - intake
  - intake-xarray
  - h5netcdf
  - geoviews
  - datashader
  - hvplot
  - pip
  - pip:
      - "git+https://github.com/intake/filesystem_spec"
      - "git+https://github.com/dask/s3fs"
# add fsspec from master in postBuild, because adding it here doesn't work, as modern pip doesn't clobber conda packages, and some package here has it as a dependency
